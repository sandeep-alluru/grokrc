[Unit]
Description=grokrc health watchdog — restart the daemon if it stops answering

[Service]
Type=oneshot
# Absolute path: a user timer inherits almost no environment.
ExecStart=/home/user/Agenthub/grok-remote-control/tools/watchdog.sh
