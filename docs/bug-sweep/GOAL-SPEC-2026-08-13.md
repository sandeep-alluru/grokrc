# Goal-spec · grokrc bug sweep · 2026-08-13

**Input (owner):** *do a bug sweep as per the agent os directives — treat this as a goal spec input create a spec checklist and work based on the spec checklist proceed now and get it started and work till there is 0 bugs in last 2 consecutive runs*

**Scope:** `https://github.com/sandeep-alluru/grokrc` working tree at `/home/clawer-samurai-1/AgentHub/grokrc`  
**Directives:** agent-os-directive 01, 03, 04, 07, 08, 09, 10, 12 (autonomous within approved scope)  
**Out of scope:** npm publish, force-push, Cloudflare, Tailscale operator sudo (HARD BLOCK if required for a product fix — document, do not invent)

---

## Done means (closed chain — zero ambiguity)

1. A **SPEC CHECKLIST** exists with every gate + a settle command.
2. **RUN-N** logs under `docs/bug-sweep/runs/` with command output summaries.
3. Every **failure** is either:
   - **REPRODUCED** → fixed with PRE-FIX / POST-FIX / ISOLATED evidence, microtest kept; or
   - **HARDENING** / **NO-REPRO** with stated why; or
   - **not a defect** / **detector wrong** (explicit).
4. **Two consecutive full verification runs** (RUN-k and RUN-k+1) each report **0 failing checks** and **0 open REPRODUCED findings**.
5. Coverage of the checklist is **to zero open gates** that the settle command can settle without owner secrets.

Not done: “mostly green”, sampling, one green run after a fix without a second full re-run.

---

## Full verification suite (one RUN)

Each item is a gate. Tick only when the settle command exits 0 in that RUN’s log.

| ID | Gate | Settle command | Class |
|----|------|----------------|-------|
| G1 | Typecheck clean | `npm run typecheck` | product |
| G2 | Lint clean | `npm run lint` | product |
| G3 | Format check | `npm run format:check` | product |
| G4 | Build | `npm run build` | product |
| G5 | Isolated test suite | `npm run test:suite` | product |
| G6 | Real-stack checks | `npm run test:real` | product (needs `grok` on PATH) |
| G7 | Guard isolation proofs | `npm run verify:guards` | product |
| G8 | Backlog file undrifted | `npm run backlog -- --check` | product |
| G9 | Stranger isolation (optional agent spawn leak) | `npm run check:stranger` | product |
| G10 | directive-check no **new** violations vs baseline | `node …/directive-check.mjs .` | process |
| G11 | Findings register: 0 open REPRODUCED | `docs/bug-sweep/FINDINGS.md` | process |

**RUN pass criteria:** G1–G11 all settle green (or G9 skip only if documented HARD BLOCK; G6 skip only if no agent — not true on this host).

---

## Working rules (from directives — non-negotiable)

- **01** Last 10% is the work; do not declare done on sampling.
- **03** Mocks are not proof of production. Real-stack gates G6/G7 matter.
- **04** ANALYSE → EVALUATE → REANALYSE → TRACE → microtest before edit.
- **07** No fix claim without PRE-FIX fail + POST-FIX pass; REPRODUCED needs ISOLATED.
- **08** No assume/probably/should; measure or mark UNKNOWN.
- **12** Autonomous within this approved goal-spec until done or HARD BLOCK.

---

## Checklist status (updated as runs complete)

- [x] S0 · Spec written + hooks installed
- [x] S1 · RUN-1 baseline (all gates measured) — `runs/RUN-1/`
- [x] S2 · Findings registered — `FINDINGS.md`
- [x] S3 · Each REPRODUCED finding fixed + evidence (F1–F7 closed)
- [x] S4 · RUN-4 full suite green — `runs/RUN-4/summary.txt` gate_failures: 0
- [x] S5 · RUN-5 full suite green consecutive — `runs/RUN-5/summary.txt` gate_failures: 0
- [x] S6 · Done report — `DONE-REPORT-2026-08-13.md`

**Done criterion met:** last two consecutive full runs (RUN-4, RUN-5) have 0 gate failures and 0 open REPRODUCED findings.

---

## Environment (measured at spec write)

See RUN-1 header in `docs/bug-sweep/runs/`.
