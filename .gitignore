node_modules/
dist/
*.tsbuildinfo

# Device tokens, push keys, pairing state — never commit
.grokrc/
devices.json
vapid.json
push-subscriptions.json

.env
.env.*
!.env.example

# Owner-local takeover dumps (session extracts, backup pointers) — not for publish
.handoff/

.DS_Store
*.log
.idea/
.vscode/
